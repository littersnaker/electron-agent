Multi-agent 多平台 Commerce Agent 增量修改文件

覆盖到原项目根目录即可。核心链路：
Amazon / TikTok Shop / Temu / 1688：API 有数据直接使用；API 未配置、失败或空结果时自动切换公开页面爬虫。

回归命令：
pnpm test:commerce-routing
pnpm test:platform-routing
pnpm test:platform-crawlers  # 真实网络冒烟测试
pnpm lint
pnpm build
